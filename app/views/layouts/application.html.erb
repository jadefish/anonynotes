<!DOCTYPE html>
<html>
  <head>
    <title><%=content_for?(:title) ? content_for(:title) : ''%>Anonynotes</title>
    <%=csrf_meta_tags%>
    <%=csp_meta_tag%>

    <%=stylesheet_link_tag 'application', media: 'all'%>
    <%=javascript_include_tag 'application'%>
  </head>

  <body>
    <%=yield%>
  </body>
</html>
